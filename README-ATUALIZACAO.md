# VistaPro — Google tag atualizado

O Google tag foi atualizado para `AW-18328641678` no `<head>` do site. O script continua assíncrono para não bloquear a renderização inicial.

As imagens, otimizações de desempenho, dados cadastrais, kits e links de checkout foram preservados.
